export interface Location {
    id: number;
    name: string;
    size: number; //size of the Location (office) expressed in meters squared 
    latitude: number;  // Latitude coordinate of the location
    longitude: number; // Longitude coordinate of the location
  }